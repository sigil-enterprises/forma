\begin{frame}[plain]
  \begin{tikzpicture}[remember picture,overlay]
    \fill[primary] (current page.south west) rectangle (current page.north east);
  \end{tikzpicture}
  \begin{center}
    \vspace{1.5cm}
    (% if content.closing and content.closing.logo %)
    \includegraphics[height=1.5cm]{ ((- content.closing.logo -)) }\\[0.8cm]
    (% endif %)
    (% if content.closing and content.closing.tagline %)
    {\color{accent}\large\itshape (( content.closing.tagline | latex_escape )) }\\[1cm]
    (% endif %)
    {\color{white}\rule{0.4\linewidth}{1pt}}\\[0.6cm]
    (% if content.closing %)
    (% if content.closing.email %)
    {\color{white}\small (( content.closing.email | latex_escape )) }\\
    (% endif %)
    (% if content.closing.website %)
    {\color{white}\small (( content.closing.website | latex_escape )) }\\
    (% endif %)
    (% endif %)
  \end{center}
\end{frame}
